// for spawn rm
export const intro = [["Welcome to my portfolio page"], ["Feel free to walk arround, more content will be added in the future"]];

// for portfolio rm
export const openWorld = [["This way plans to be the JRPG part,But not yet finish"]];
export const undevelopment = [["Not sure how I will use this area so just leave it first"]];

// for diary